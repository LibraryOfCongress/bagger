# This is a scratch pad for now and will eventually become a formal README
# with release notes and detailed installation instructions


PREREQUISITES:
You must have the PostgreSQL 8.2 database running
Try:  /usr/sbin/svcadm enable postgresql:version_82

HOWTOS:
# Create PostgreSQL DB cluster
CREATE_POSTGRES_CLUSTER="/usr/postgres/8.2/bin/initdb -D /var/postgres/8.2/data"

# Start Postgresql
START_POSTGRES="/usr/sbin/svcadm enable postgresql:version_82"


